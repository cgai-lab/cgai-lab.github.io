import React from 'react';

import Layout from '@components/Layout';
import TopSection from '@components/TopSection';

const PublicationsPage = () => {
  return (
    <Layout>
      <TopSection title="Publications" />
    </Layout>
  );
};

export default PublicationsPage;
