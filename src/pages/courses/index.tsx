import React from 'react';

import Layout from '@components/Layout';
import TopSection from '@components/TopSection';

const CoursesPage = () => {
  return (
    <Layout>
      <TopSection title="Courses" />
    </Layout>
  );
};

export default CoursesPage;
